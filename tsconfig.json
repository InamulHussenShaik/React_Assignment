{
  "files": [],
  "references": [
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.node.json"
    }
  ],
  "compilerOptions": {
    "baseUrl": "./src",  // Set baseUrl to ./src to resolve paths correctly
    "paths": {
      "@/*": ["./src/*"]  // Ensure alias @ resolves to ./src
    }
  }
}
